# Access Control Lists (ACLs): Network Security 🛡️

একটি নেটওয়ার্কে কোন ট্রাফিক ঢুকতে পারবে আর কোনটি পারবে না, তা নিয়ন্ত্রণ করার সবচেয়ে প্রাথমিক এবং শক্তিশালী মাধ্যম হলো **ACL (Access Control List)**। এটি অনেকটা সিকিউরিটি গার্ডের মতো কাজ করে, যার কাছে একটি লিস্ট থাকে যে কে ভেতরে যেতে পারবে আর কে পারবে না।

---

## ১. ACL কী এবং কেন প্রয়োজন? (Use Cases)
ACL হলো একগুচ্ছ রুলস (Rules) যা রাউটার বা সুইচের ইন্টারফেসে প্রয়োগ করা হয় ট্রাফিক ফিল্টার করার জন্য।
- **Security:** আনঅথরাইজড এক্সেস বন্ধ করা।
- **Traffic Control:** নির্দিষ্ট সাবনেট বা আইপি-কে ইন্টারনেটে যেতে বাধা দেওয়া।
- **Efficiency:** অপ্রয়োজনীয় ট্রাফিক ব্লক করে ব্যান্ডউইথ বাঁচানো।

---

## ২. ACL এর প্রকারভেদ (Types of ACLs)

### ক. Standard ACL (স্ট্যান্ডার্ড এসিএল):
এটি কেবল **Source IP Address** দেখে ট্রাফিক ফিল্টার করে।
- **Range:** ১ - ৯৯ এবং ১৩০০ - ১৯৯৯।
- **Placement:** এটি গন্তব্যের (Destination) যত কাছে সম্ভব তত কাছে বসানো উচিত।

### খ. Extended ACL (এক্সটেন্ডেড এসিএল):
এটি অনেক বেশি অ্যাডভান্সড। এটি Source IP, Destination IP, Protocol (TCP/UDP), এবং পোর্ট নম্বর দেখে ফিল্টার করতে পারে।
- **Range:** ১০০ - ১৯৯ এবং ২০০০ - ২৬৯৯।
- **Placement:** এটি সোর্সের (Source) যত কাছে সম্ভব তত কাছে বসানো উচিত যেন নেটওয়ার্কে অপ্রয়োজনীয় ডাটা না ঢকে।

### গ. Named ACL:
নম্বরের বদলে নাম ব্যবহার করে যে এসিএল তৈরি করা হয়। এটি মডিফাই করা সহজ।

---

## ৩. ওয়াইল্ডকার্ড মাস্ক (Wildcard Masks)
সাবনেট মাস্কের ঠিক উল্টো হলো ওয়াইল্ডকার্ড মাস্ক। এটি এসিএল-এ নির্দিষ্ট আইপি রেঞ্জ বোঝাতে ব্যবহৃত হয়।
- **Subnet Mask:** `255.255.255.0`
- **Wildcard Mask:** `0.0.0.255` (এর মানে হলো শেষ অক্টেটটি যা ইচ্ছা হতে পারে)।

---

## ৪. ACL এর কাজের নিয়ম (How ACL Works)
রাউটার যখন কোনো প্যাকেট পায়, সে এসিএল লিস্টের উপর থেকে নিচ পর্যন্ত চেক করে:
1.  **Top-down Processing:** প্রথম যে রুলের সাথে ম্যাচ করবে, সেখানেই সে থেমে যাবে (পাস বা ব্লক করবে)।
2.  **Implicit Deny:** লিস্টের শেষে একটি ইনভিজিবল রুল থাকে যার মানে হলো "প্যাকেট যদি উপরের কোনো রুলের সাথে না মেলে, তবে সেটি ব্লক হবে"। তাই এসিএল-এ অন্তত একটি **Permit** রুল থাকতে হয়।

---

## ৫. ACL এর সেরা প্র্যাকটিস (Best Practices)
- **Top Down:** সবচেয়ে স্পেসিফিক রুলগুলোকে উপরে রাখুন।
- **Documentation:** রুলগুলোর সাথে কমেন্ট বা ডেসক্রিপশন যুক্ত করুন।
- **Implicit Deny:** মনে রাখবেন যে রুল ম্যাচ না করলে সেটি ব্লক হয়ে যাবে, তাই প্রয়োজনীয় ট্রাফিক যেন পারমিট পায় তা নিশ্চিত করুন।
- **Placement:** স্ট্যান্ডার্ড এসিএল গন্তব্যে আর এক্সটেন্ডেড এসিএল সোর্সের কাছে বসান।

---

## ৬. ডেভঅপস পারসপেক্টিভ: ফায়ারওয়াল ও IaC
- **Stateless Firewall:** ট্র্যাডিশনাল এসিএল হলো 'Stateless'। অর্থাৎ সে প্রতিটি প্যাকেটকে আলাদাভাবে চেক করে। আধুনিক ফায়ারওয়ালগুলো (যেমন: AWS Security Groups) হলো 'Stateful', যা সেশন মনে রাখতে পারে।
- **Infrastructure as Code (IaC):** টেরাফর্ম (Terraform) বা এনসিবল (Ansible) ব্যবহার করে এখন অটোমেটেড উপায়ে এসিএল রুল পুশ করা হয় যেন হিউম্যান এরর না হয়।
- **Micro-segmentation:** ক্লাউড আর্কিটেকচারে প্রতিটি মাইক্রোসার্ভিসের জন্য আলাদা আলাদা টাইট এসিএল ব্যবহার করা হয় জিরো ট্রাস্ট সিকিউরিটি নিশ্চিত করতে।

---

::: tip মনে রাখুন!
একটি ভুল এসিএল রুল পুরো নেটওয়ার্ককে অচল করে দিতে পারে। তাই প্রোডাকশন রাউটারে রুল পুশ করার আগে সবসময় ল্যাব বা স্টেজিং এনভায়রনমেন্টে টেস্ট করা উচিত। 
:::
