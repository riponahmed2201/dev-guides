# 📚 Level 1: Beginner (Foundation)

Python এর প্রাথমিক ধারণাগুলো এখান থেকেই শুরু হয়।

## [1. Python Basics](./basics)
Python এর প্রাথমিক ধারণাগুলো এখান থেকেই শুরু হয়। এই অংশে আমরা শিখবো:
- Python কি এবং কেন শিখবে
- Installation এবং Setup
- First Program (Hello World)
- Interpreter এবং Comments 

## 2. Variables & Data Types
- Variables declaration এবং naming rules
- Basic data types (`int`, `float`, `string`, `boolean`)
- Type conversion এবং casting
- Constants
- Multiple assignment

## 3. Operators
- **Arithmetic**: `+`, `-`, `*`, `/`, `//`, `%`, `**`
- **Comparison**: `==`, `!=`, `>`, `<`, `>=`, `<=`
- **Logical**: `and`, `or`, `not`
- **Assignment**: `=`, `+=`, `-=`, etc.
- **Bitwise**, **Identity** (`is`), **Membership** (`in`)

## 4. Strings
- String creation এবং indexing
- **Slicing**: `my_string[start:stop:step]`
- **Methods**: `upper()`, `lower()`, `strip()`, `split()`, `join()`
- **Formatting**: f-strings (Recommended), `format()`, `%`

## 5. Input/Output
- `input()` function
- `print()` function এবং formatting parameters (`sep`, `end`)

## 6. Control Flow
- `if`, `elif`, `else` statements
- Nested conditionals
- Ternary operator (`a if condition else b`)
- **Match-case** (Python 3.10+)

## 7. Loops
- `for` loop
- `while` loop
- `break`, `continue`, `pass`
- Loop patterns

## 8. Data Structures - Basics
### Lists
- Indexing, Slicing
- Methods: `append()`, `insert()`, `remove()`, `pop()`
- **List comprehension** basics

### Tuples
- Immutability
- Unpacking

### Dictionaries
- Key-Value pairs
- Methods: `get()`, `keys()`, `values()`, `items()`

### Sets
- Unique elements
- Operations: Union, Intersection

## 9. Functions
- Definition এবং calling
- Parameters (`*args`, `**kwargs`)
- Return values
- Docstrings

---

### 🛠 Beginner Projects
1. Calculator
2. To-Do List application
3. Number guessing game
4. Password generator
5. Simple quiz game
