# Docker Hub: The Social Media of Docker 🐳

**Docker Hub** হলো পৃথিবীর সবচেয়ে বড় এবং জনপ্রিয় ক্লাউড-বেসড কন্টেইনার রেজিস্ট্রি। এটি মূলত ডকার ইমেজের একটি লাইব্রেরি বা স্টোরহাউজ, যেখান থেকে আমরা ইমেজ ডাউনলোড (Pull) করি এবং আমাদের তৈরি করা ইমেজ আপলোড (Push) করে থাকি।

---

## ১. Docker Hub অ্যাকাউন্ট তৈরি করা

ডকার হাব ব্যবহার করার জন্য একটি অ্যাকাউন্ট থাকা জরুরি। 
- **কিভাবে:** [hub.docker.com](https://hub.docker.com/)-এ গিয়ে একটি ইউনিক ইউজারনেম এবং ইমেইল দিয়ে সাইন-আপ করুন।
- **কেন:** আপনার নিজের ইমেজ আপলোড করতে এবং প্রাইভেট রিপোজিটরি ব্যবহার করতে অ্যাকাউন্টের প্রয়োজন হয়।

---

## ২. রিপোজিটরি (Public vs Private)

ডকার হাবে আপনি দুই ধরনের রিপোজিটরি বা স্টোরেজ ব্যবহার করতে পারেন:

- **Public Repositories:** এগুলো সবার জন্য উন্মুক্ত। যে কেউ আপনার ইমেজ সার্চ করে ডাউনলোড করতে পারবে। এটি ওপেন সোর্স প্রজেক্টের জন্য সেরা।
- **Private Repositories:** এগুলো শুধুমাত্র আপনি বা আপনার টিমের মেম্বাররা দেখতে পাবে। কোম্পানির সিক্রেট প্রজেক্টের জন্য এটি ব্যবহার করা হয় (ফ্রি অ্যাকাউন্টে সাধারণত একটি প্রাইভেট রিপোজিটরি পাওয়া যায়)।

---

## ৩. ইমেজ সার্চ করা (Search)

সঠিক ইমেজ খুঁজে বের করা একটি দক্ষতা। ডকার হাবে সার্চ করার সময় নিচের বিষয়গুলো খেয়াল রাখা উচিত:

- **Official Images:** এগুলো ডকার টিমের দ্বারা ভেরিফাইড এবং সবচেয়ে নিরাপদ (যেমন: `nginx`, `redis`)।
- **Verified Publishers:** বড় কোম্পানিগুলোর (যেমন: Microsoft, Amazon, MongoDB) তৈরি করা ইমেজ যেখানে একটি ব্লু-টিক মার্ক থাকে।

---

## ৪. ইমেজ রেটিং এবং ডাউনলোড

কোন ইমেজটি ভালো সেটি বোঝার উপায়:
- **Stars:** কতজন ইউজার ইমেজটিকে পছন্দ করেছে।
- **Downloads:** কতবার ইমেজটি ডাউনলোড হয়েছে।
- **Last Updated:** ইমেজটি কতদিন আগে আপডেট করা হয়েছে (পুরানো ইমেজ ব্যবহার না করাই ভালো)।

---

## ৫. Automated Builds

ডকার হাবের একটি চমৎকার ফিচার হলো **Automated Build**। আপনি যদি আপনার GitHub বা Bitbucket রিপোজিটরির সাথে ডকার হাব লিঙ্ক করে দেন, তবে কোড পুশ করার সাথে সাথেই ডকার হাব অটোমেটিক নতুন ইমেজ বিল্ড করে ফেলবে। একে **CI (Continuous Integration)**-এর একটি অংশ বলা হয়।

---

## কমান্ড সামারি টেবিল

| Task | Command |
| :--- | :--- |
| Login to Hub | `docker login` |
| Search Image | `docker search <term>` |
| Pull Image | `docker pull <image>` |
| Push Image | `docker push <user>/<image>` |

---

> [!IMPORTANT]
> প্রোডাকশনে সবসময় **Official Images** অথবা **Verified Publishers**-এর ইমেজ ব্যবহার করার চেষ্টা করবেন। এতে সিকিউরিটি রিস্ক অনেক কমে যায়।
