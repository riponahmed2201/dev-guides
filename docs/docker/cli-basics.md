# Docker CLI Basics 💻

Docker-এর সাথে কাজ করার প্রধান মাধ্যম হলো এর **CLI (Command Line Interface)**। ডকার কমান্ডগুলো খুব গোছানো এবং লজিক্যাল। এই গাইডে আমরা ডকার কমান্ডের গঠন এবং কিছু বেসিক কমান্ড নিয়ে আলোচনা করবো।

---

## ১. Docker কমান্ডের গঠন (Command Structure)

মডার্ন ডকার কমান্ডগুলো একটি নির্দিষ্ট প্যাটার্ন অনুসরণ করে:

```bash
docker <management-command> <sub-command> [options]
```

**উদাহরণ:**
```bash
docker container run --name my-web -d nginx
```
- `docker`: মূল কমান্ড।
- `container`: ম্যানেজমেন্ট কমান্ড (কি নিয়ে কাজ করছি)।
- `run`: সাব-কমান্ড (কি করতে চাইছি)।
- `--name my-web`, `-d`: অপশন বা ফ্ল্যাগ।
- `nginx`: আর্গুমেন্ট (কোন ইমেজ ব্যবহার করবো)।

> [!NOTE]
> কিছু পুরনো কমান্ড সরাসরি কাজ করে (যেমন: `docker run` এর বদলে `docker container run`)। এদেরকে "Legacy Commands" বলা হয়, তবে মডার্ন স্ট্রাকচার ব্যবহার করা ভালো।

---

## ২. Help System (সাহায্য পাওয়ার উপায়)

ডকারের সব কমান্ড মুখস্থ রাখার প্রয়োজন নেই। ডকার নিজেই আপনাকে সাহায্য করবে।

- **সামগ্রিক সাহায্য:**
  ```bash
  docker --help
  ```
- **নির্দিষ্ট কমান্ডের সাহায্য:**
  ```bash
  docker container --help
  docker image pull --help
  ```
এটি আপনাকে সেই কমান্ডের সব এভেইলেবল ফ্ল্যাগ এবং ব্যবহার পদ্ধতি দেখিয়ে দেবে।

---

## ৩. ডকার ভার্সন এবং ইনফো (Basic Info)

আপনার মেশিনের ডকার এনভায়রনমেন্ট সম্পর্কে জানার জন্য এই কমান্ডগুলো ব্যবহৃত হয়:

- **ডকার ভার্সন চেক করা:**
  ```bash
  docker version
  ```
  এটি ক্লায়েন্ট এবং সার্ভার (Daemon) উভয়ের বিস্তারিত ভার্সন তথ্য দেয়।

- **সিস্টেম ওয়াইড ইনফরমেশন:**
  ```bash
  docker info
  ```
  এটি আপনার মেশিনে কয়টি কন্টেইনার রানিং আছে, কয়টি ইমেজ আছে, স্টোরেজ ড্রাইভার কী ব্যবহার হচ্ছে ইত্যাদি বিস্তারিত তথ্য দেয়।

---

## ৪. Docker Registry-তে লগইন (Auth)

Docker Hub বা অন্য কোনো প্রাইভেট রেজিস্ট্রিতে ইমেজ পুশ বা পুল (Private images-এর ক্ষেত্রে) করার জন্য লগইন করতে হয়।

- **লগইন করা:**
  ```bash
  docker login
  ```
  এটি আপনার ইউজারনেম এবং পাসওয়ার্ড (বা এক্সেস টোকেন) চাইবে।

- **লগআউট করা:**
  ```bash
  docker logout
  ```

---

## ৫. Common Flags (সাধারণ কিছু ফ্ল্যাগ)

সব কমান্ডেই কিছু ফ্ল্যাগ বারবার ব্যবহৃত হয়:
- `--help`: কমান্ডের হেল্প দেখতে।
- `-v` বা `--version`: ভার্সন দেখতে।
- `-d`: ডিটাচড মোড (Detached mode) - ব্যাকগ্রাউন্ডে চালানোর জন্য।
- `-it`: ইন্টারঅ্যাক্টিভ মোড (Interactive mode) - কন্টেইনারের ভেতরে ইনপুট দেওয়ার জন্য।
- `--name`: কন্টেইনারের একটি নির্দিষ্ট নাম দেওয়ার জন্য।

---

## ৬. Command Categories

ডকার কমান্ডগুলোকে কয়েকটি বড় ক্যাটাগরিতে ভাগ করা যায়:
১. **Container commands:** কন্টেইনার ম্যানেজ করার জন্য (`docker container ...`)
২. **Image commands:** ইমেজ ম্যানেজ করার জন্য (`docker image ...`)
৩. **Network commands:** নেটওয়ার্ক সেটআপের জন্য (`docker network ...`)
৪. **Volume commands:** ডেটা ভলিউম ম্যানেজ করার জন্য (`docker volume ...`)

---

> [!TIP]
> ডকার কমান্ড টাইপ করার সময় **Tab** কি প্রেস করলে অনেক সময় কমান্ড অটো-কমপ্লিট হয়, যা আপনার কাজকে সহজ করে দেবে।

> [!IMPORTANT]
> পরবর্তী ধাপে আমরা শিখবো কীভাবে **Docker Images** নিয়ে প্রফেশনালি কাজ করা যায়।
