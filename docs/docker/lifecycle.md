# Container Lifecycle 🔄

Docker কন্টেইনারের একটি নির্দিষ্ট জীবনচক্র (Lifecycle) আছে। একটি কন্টেইনার তৈরি থেকে শুরু করে ডিলিট হওয়া পর্যন্ত বিভিন্ন ধাপের মধ্য দিয়ে যায়। এই ধাপগুলো বুঝতে পারলে আপনি ডকারকে আরও ভালোভাবে কন্ট্রোল করতে পারবেন।

---

## ১. কন্টেইনারের জীবনচক্রের ধাপসমূহ (Workflow Phases)

একটি কন্টেইনার মূলত নিচের পাঁচটি প্রধান ধাপ বা প্রক্রিয়ার মধ্য দিয়ে যায়:

১. **Create:** একটি ইমেজ থেকে কন্টেইনার মেটাডেটা তৈরি করা (`docker create`)।
২. **Start:** তৈরি করা কন্টেইনারটি মেমোরিতে লোড করে রান করা (`docker start`)।
৩. **Run:** কন্টেইনারটি যখন এক্টিভ থাকে এবং তার ভেতরের প্রসেসগুলো চলছে (`docker run` = create + start)।
৪. **Stop:** রানিং প্রসেস বন্ধ করে কন্টেইনারটিকে থামিয়ে দেওয়া (`docker stop`)।
৫. **Remove:** কন্টেইনারটি পুরোপুরি ডিলিট করে দেওয়া (`docker rm`)।

---

## ২. কন্টেইনারের স্টেটসমূহ (Container States)

টার্মিনালে `docker ps -a` দিলে আপনি কন্টেইনারের বিভিন্ন স্টেট (Status) দেখতে পান। প্রধান স্টেটগুলো হলো:

- **Created:** কন্টেইনারটি তৈরি হয়েছে কিন্তু কখনও স্টার্ট করা হয়নি।
- **Running:** কন্টেইনারটি বর্তমানে চালু আছে এবং কাজ করছে।
- **Paused:** কন্টেইনারের প্রসেসগুলো সাময়িকভাবে থামিয়ে রাখা হয়েছে (CPU ব্যবহার করছে না)।
- **Exited (Stopped):** কন্টেইনারটি বন্ধ হয়ে গেছে (হয়তো এর এর কাজ শেষ অথবা ম্যানুয়ালি থামানো হয়েছে)।
- **Restarting:** কোনো এররের কারণে ডকার কন্টেইনারটিকে পুনরায় রান করার চেষ্টা করছে।

---

## ৩. Container vs Image: মূল পার্থক্য

অনেকেই এই দুটির মধ্যে গুলিয়ে ফেলেন। একটি সহজ তুলনা দেখা যাক:

| Feature | Image | Container |
| :--- | :--- | :--- |
| **Nature** | রিড-অনলি (Static) | রিড-রাইট (Dynamic) |
| **Analogy** | এটি একটি ক্লাস বা ব্লু-প্রিন্ট | এটি ঐ ক্লাসের একটি অবজেক্ট |
| **Storage** | ডিস্কে জায়গা নেয় | রানটাইম মেমোরি ও ডিস্ক লেয়ার নেয় |
| **Change** | ইমেজ পরিবর্তন করা যায় না (নতুন ভার্সন লাগে) | কন্টেইনারের ভেতরে ফাইল পরিবর্তন করা যায় |

---

## ৪. Ephemeral Nature (স্বল্পস্থায়ী প্রকৃতি)

Docker কন্টেইনারগুলো ডিফল্টভাবে **Ephemeral** হয়। এর মানে হলো:
- কন্টেইনারটি যত সময় রানিং থাকবে, তার ভেতরকার ডেটা তত সময় থাকবে।
- যদি আপনি কন্টেইনারটি রিমুভ (`rm`) করে দেন, তবে তার ভেতরে তৈরি করা সব ফাইল বা ডেটা চিরতরে মুছে যাবে।

অর্থাৎ, কন্টেইনারের মৃত্যু মানে তার হার্ডড্রাইভেরও মৃত্যু।

---

## ৫. Data Persistence Challenges

কন্টেইনারের এই Ephemeral স্বভাবের কারণে কিছু চ্যালেঞ্জ তৈরি হয়:
- ডাটাবেস কন্টেইনারে ডেটা রাখলে তা রিমুভ করার পর ডেটা হারিয়ে যাওয়ার ভয় থাকে।
- কনফিগারেশন ফাইলগুলো বারবার কন্টেইনারের ভেতরে সেট করা কষ্টকর।

**সমাধান:** এই সমস্যা সমাধানের জন্যই ডকার **Volumes** এবং **Bind Mounts** ব্যবহার করে, যা কন্টেইনারের বাইরে ডেটা সেভ করে রাখে।

---

> [!IMPORTANT]
> মনে রাখবেন, **Docker Best Practice** হলো: কন্টেইনারকে এমনভাবে ডিজাইন করা যেন সেটি যেকোনো সময় বন্ধ বা রিমুভ করলে আপনার গুরুত্বপূর্ণ ডেটার কোনো ক্ষতি না হয়।