# Docker Introduction 🐋

সফটওয়্যার ডেভেলপমেন্টের জগতে Docker একটি বিপ্লবী নাম। এটি অ্যাপ্লিকেশন লঞ্চ করার প্রসেসকে আমূল বদলে দিয়েছে। এই গাইডে আমরা ডকারের গভীরে প্রবেশ করবো।

---

## ১. Docker কী? (What is Docker?)

**Docker** হলো একটি ওপেন-সোর্স প্ল্যাটফর্ম যা অ্যাপ্লিকেশনগুলোকে কন্টেইনারের মধ্যে তৈরি (Build), শিপ (Ship) এবং রান (Run) করতে সহায়তা করে। এটি ডেভেলপমেন্ট এনভায়রনমেন্ট এবং প্রোডাকশন এনভায়রনমেন্টের মধ্যকার ব্যবধান কমিয়ে আনে।

সহজ ভাষায়, ডকার হলো এমন একটি টুল যা আপনার অ্যাপ এবং তার প্রয়োজনীয় সব লাইব্রেরিকে একটি বাক্সে (Container) ভরে দেয়, যাতে সেটি যেকোনো কম্পিউটারে একইভাবে চলে।

---

## ২. Docker এর ইতিহাস (History of Docker)

- **২০১০:** Solomon Hykes এবং তার টিম ফ্রান্সে **dotCloud** নামে একটি PaaS (Platform as a Service) কোম্পানি শুরু করেন।
- **২০১৩:** dotCloud তাদের ইন্টারনাল প্রজেক্ট হিসেবে তৈরি করা 'Docker' কে ওপেন-সোর্স হিসেবে প্রকাশ করে।
- **২০১৪:** Docker ১.০ ভার্সন রিলিজ হয় এবং এটি অবিশ্বাস্য দ্রুতগতিতে জনপ্রিয়তা পেতে শুরু করে।
- **বর্তমান:** আজ ডকার কন্টেইনারাইজেশনের জন্য ইন্ডাস্ট্রির স্ট্যান্ডার্ড হয়ে দাঁড়িয়েছে।

---

## ৩. Docker Architecture Overview

Docker একটি **Client-Server** আর্কিটেকচার অনুসরণ করে। এর প্রধান তিনটি অংশ হলো:

১. **Docker Client:** এটি হলো সেই মাধ্যম (CLI), যার মাধ্যমে আমরা ডকারকে কমান্ড দিই (যেমন: `docker run`)।
২. **Docker Daemon (Host):** এটি ব্যাকগ্রাউন্ডে চলে এবং কন্টেইনার তৈরি, রান এবং ম্যানেজ করার আসল কাজগুলো করে।
৩. **Docker Registry:** এটি হলো একটি স্টোরেজ যেখানে ডকার ইমেজগুলো রাখা হয় (যেমন: Docker Hub)।

---

## ৪. Docker Ecosystem

ডকার শুধুমাত্র একটি টুল নয়, এটি একটি বিশাল ইকোসিস্টেম। এর প্রধান উপাদানগুলো হলো:
- **Docker Engine:** মূল কন্টেইনারাইজেশন টেকনোলজি।
- **Docker Hub:** ইমেজ শেয়ার এবং ম্যানেজ করার ক্লাউড প্লেস।
- **Docker Compose:** মাল্টি-কন্টেইনার অ্যাপ চালানোর টুল।
- **Docker Swarm:** ডকারের নিজস্ব অর্কেস্ট্রেশন টুল।
- **Docker Desktop:** উইন্ডোজ বা ম্যাকের জন্য সহজ ইন্টারফেস।

---

## ৫. Docker Editions

ডকার মূলত দুটি ভার্সনে পাওয়া যায়:
১. **Docker Community Edition (CE):** এটি ফ্রি এবং ডেভেলপার বা ছোট টিমের জন্য একদম উপযুক্ত।
২. **Docker Enterprise (EE):** এটি বড় কোম্পানিগুলোর জন্য তৈরি, যেখানে সিকিউরিটি এবং সাপোর্টকে বেশি গুরুত্ব দেওয়া হয় (বর্তমানে এটি Mirantis দ্বারা পরিচালিত)।

---

## ৬. Docker কেন এত জনপ্রিয়? (Why Docker is popular?)

১. **Speed:** ভার্চুয়াল মেশিনের তুলনায় ডকার কন্টেইনার অনেক দ্রুত স্টার্ট হয়।
২. **Standardization:** এটি অ্যাপ ডেভেলপমেন্টের একটি স্ট্যান্ডার্ড ফরম্যাট তৈরি করে।
৩. **Version Control for Infrastructure:** ইমেজের মাধ্যমে আপনি ইনফ্রাস্ট্রাকচারের ভার্সন কন্ট্রোল করতে পারেন।
৪. **Cost Effective:** কম হার্ডওয়্যারে অনেক বেশি অ্যাপ চালানো যায়।

---

## ৭. Docker Alternatives (বিকল্পসমূহ)

ডকার সেরা হলেও এর কিছু বিকল্প আছে যা বর্তমান মার্কেটে ব্যবহৃত হয়:
- **Podman:** এটি একটি ডেমোন-লেস কন্টেইনার ইঞ্জিন (RedHat-এর তৈরি)।
- **LXC (Linux Containers):** এটি আরও লো-লেভেল কন্টেইনার টেকনোলজি।
- **containerd / runc:** এগুলো ডকারের ভেতরের কম্পোনেন্ট যা এখন আলাদাভাবেও ব্যবহার করা হয় (বিশেষ করে Kubernetes-এ)।

---

## ৮. Industry Adoption

বর্তমানে ছোট স্টার্টআপ থেকে শুরু করে Google, Amazon, এবং Netflix এর মতো জায়ান্ট কোম্পানিগুলো ডকার ব্যবহার করছে। ৯০% এর বেশি আধুনিক ক্লাউড আর্কিটেকচার এবং CI/CD পাইপলাইন এখন ডকারের ওপর ভিত্তি করে তৈরি।

---

> [!TIP]
> পরবর্তী ধাপে আমরা দেখবো কীভাবে ডকারের ইন্টারনাল আর্কিটেকচার কাজ করে এবং কীভাবে এটি আপনার মেশিনে ইন্সটল করবেন।
