# Docker Architecture 🏗️

Docker-এর কাজের পদ্ধতি বোঝার জন্য এর আর্কিটেকচার বোঝা খুবই জরুরি। Docker একটি **Client-Server** আর্কিটেকচার ব্যবহার করে যেখানে ক্লায়েন্ট এবং ডেমন (Daemon) একে অপরের সাথে যোগাযোগ করে।

---

## ১. Client-Server Architecture

Docker মূলত তিনটি প্রধান অংশে বিভক্ত:

১. **Docker Client:** আমরা যখন কমান্ড লাইনে (Terminal) `docker run` বা `docker build` লিখি, তখন আমরা আসলে Docker Client ব্যবহার করছি। ক্লায়েন্ট এই কমান্ডগুলো Docker Daemon-এর কাছে পাঠায়।
২. **Docker Host (Daemon):** একে **dockerd** বলা হয়। এটি ব্যাকগ্রাউন্ডে চলে এবং কন্টেইনার তৈরি, স্টার্ট বা ইমেজের দেখাশোনা করে।
৩. **Docker Registry:** এটি একটি সেন্ট্রাল রিপোজিটরি যেখানে ইমেজগুলো জমা থাকে (যেমন: Docker Hub)।

---

## ২. Docker Daemon (dockerd)

Docker Daemon হলো Docker Engine-এর হার্ট। এটি API রিকোয়েস্ট শোনে এবং ডকার অবজেক্ট যেমন ইমেজ, কন্টেইনার, নেটওয়ার্ক এবং ভলিউম ম্যানেজ করে। একটি ডকার ডেমোন অন্য ডেমোনের সাথেও যোগাযোগ করতে পারে।

---

## ৩. Docker Client

এটি ডকার ইউজারদের জন্য প্রাইমারি ইন্টারফেস। আপনি যখন `docker` কমান্ড দেন, ক্লায়েন্ট সেই কমান্ডটি `dockerd`-কে পাঠিয়ে দেয়। ক্লায়েন্ট চাইলে একই সাথে একাধিক ডেমোনের সাথে যোগাযোগ করতে পারে।

---

## ৪. REST API

Docker Client এবং Daemon-এর মধ্যে যোগাযোগ হয় **REST API**-এর মাধ্যমে। ডকার ক্লায়েন্ট আসলে এই API কল করে ডেমোনকে ইনস্ট্রাকশন দেয়। এর ফলে আপনি চাইলে রিমোটলিও অন্য কোনো সার্ভারের ডকার ম্যানেজ করতে পারেন।

---

## ৫. Docker Objects

ডকার ব্যবহারের সময় আমরা মূলত কিছু অবজেক্ট নিয়ে কাজ করি:

- **Images:** এটি একটি রিড-অনলি টেমপ্লেট যা কন্টেইনার তৈরির জন্য ব্যবহার করা হয়। (যেমন: একটি উবুন্টু ইমেজ)।
- **Containers:** ইমেজের একটি রানিং ইনস্ট্যান্স হলো কন্টেইনার। এটি সম্পূর্ণ আইসোলেটেড এনভায়রনমেন্ট।
- **Networks:** কন্টেইনারগুলো একে অপরের সাথে কীভাবে যোগাযোগ করবে তার জন্য নেটওয়ার্ক ব্যবহৃত হয়।
- **Volumes:** কন্টেইনার ডিলিট হয়ে গেলেও ডেটা সেভ রাখার জন্য ভলিউম ব্যবহার করা হয়।

---

## ৬. Docker Engine Components

মডার্ন ডকার ইঞ্জিন শুধুমাত্র একটি সিঙ্গেল টুল নয়, এটি বেশ কিছু স্তরে বিভক্ত:

### ক. containerd
আগে ডকার ডেমোন একাই সব কাজ করত। এখন ইমেজ ম্যানেজমেন্ট, কন্টেইনার লাইফসাইকেল এবং পুশ/পুল করার কাজগুলো **containerd** হ্যান্ডেল করে। এটি একটি ইন্ডাস্ট্রি স্ট্যান্ডার্ড কন্টেইনার রানটাইম।

### খ. runc
যখন একটি কন্টেইনার স্টার্ট করার প্রয়োজন হয়, তখন `containerd` আসলে **runc**-কে কল করে। `runc` হলো একটি অত্যন্ত হালকা টুল যা অপারেটিং সিস্টেমের লেভেলে গিয়ে কন্টেইনারটি তৈরি করে।

---

## ৭. Docker Registry

Docker Registry হলো ইমেজের লাইব্রেরি।
- **Public Registry:** Docker Hub হলো সবচেয়ে বড় পাবলিক রেজিস্ট্রি।
- **Private Registry:** কোম্পানিগুলো নিজের নিরাপত্তার জন্য নিজেদের প্রাইভেট রেজিস্ট্রি (যেমন: Google Container Registry, Azure Container Registry) ব্যবহার করে।

---

## কাজের প্রবাহ (Workflow Example)
১. আপনি কমান্ড দিলেন: `docker run hello-world`
২. **Client** এটি **Daemon**-কে পাঠালো।
৩. **Daemon** দেখলো লোকাল মেশিনে ইমেজটি নেই।
৪. সে **Registry** থেকে ইমেজটি `pull` করলো।
৫. **Daemon** একটি নতুন **Container** তৈরি করলো এবং সেটি রান করলো।

---

> [!IMPORTANT]
> ডকার আর্কিটেকচার বোঝার মাধ্যমে আপনি আরও সহজে এরোর ডিবাগ করতে পারবেন এবং বুঝতে পারবেন ব্যাকগ্রাউন্ডে কী ঘটছে।