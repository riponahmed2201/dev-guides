# Container Interaction 💡

একটি কন্টেইনার যখন রানিং থাকে, তখন তার ভেতরে কী হচ্ছে তা জানা, সেটির ফাইল এক্সেস করা এবং রিসোর্স ব্যবহারের খবর রাখা খুবই জরুরি। এই গাইডে আমরা কন্টেইনারের সাথে ইন্টারঅ্যাক্ট করার প্রধান কমান্ডগুলো শিখবো।

---

## ১. কন্টেইনারের ভেতরে প্রবেশ করা (Exec)

রানিং কোনো কন্টেইনারের ভেতরে ম্যানুয়ালি কোনো কমান্ড চালাতে বা সেটির টার্মিনাল এক্সেস করতে `docker exec` ব্যবহার করা হয়।

- **কন্টেইনারের ব্যাশ (Bash) টার্মিনালে ঢুকতে:**
  ```bash
  docker exec -it <container_id_or_name> bash
  ```
  (যদি `bash` না থাকে তবে `sh` ব্যবহার করুন)। এটি আপনাকে কন্টেইনারের কমান্ড লাইনে নিয়ে যাবে, যেখান থেকে আপনি ফাইল দেখতে বা পরিবর্তন করতে পারবেন।

---

## ২. লগ দেখা (Logs)

কন্টেইনারটি ব্যাকগ্রাউন্ডে কী আউটপুট দিচ্ছে বা কোনো এরর হচ্ছে কি না তা দেখার জন্য:

- **সব লগ দেখতে:**
  ```bash
  docker logs <container_id_or_name>
  ```
- **রিয়েল-টাইম লগ দেখতে (Live Update):**
  ```bash
  docker logs -f <container_id_or_name>
  ```
  (এখানে `-f` মানে Follow, যা নতুন লগ আসার সাথে সাথে টার্মিনালে দেখাবে)।

---

## ৩. রিসোর্স ইউসেজ দেখা (Stats & Top)

- **CPU, Memory এবং Network ব্যবহারের লাইভ আপডেট দেখতে:**
  ```bash
  docker stats
  ```
  এটি আপনার সিস্টেমে চলা সব কন্টেইনারের পারফরম্যান্স ড্যাশবোর্ড দেখাবে।
- **একটি নির্দিষ্ট কন্টেইনারের ভেতরের প্রসেসগুলো দেখতে:**
  ```bash
  docker top <container_name_or_id>
  ```

---

## ৪. কন্টেইনারের বিস্তারিত তথ্য (Inspect)

কন্টেইনারটির আইপি এড্রেস, মাউন্ট হওয়া ভলিউম, এনভায়রনমেন্ট ভেরিয়েবল এবং অন্যান্য সব টেকনিক্যাল তথ্য JSON ফরম্যাটে দেখতে:
```bash
docker inspect <container_id_or_name>
```

---

## ৫. ফাইল কপি করা (Copy)

আপনার হোস্ট মেশিন (কম্পিউটার) থেকে কন্টেইনারে অথবা কন্টেইনার থেকে হোস্ট মেশিনে ফাইল আদান-প্রদান করতে:

- **হোস্ট থেকে কন্টেইনারে কপি:**
  ```bash
  docker cp index.html <container_name>:/usr/share/nginx/html/
  ```
- **কন্টেইনার থেকে হোস্টে কপি:**
  ```bash
  docker cp <container_name>:/app/logs.txt ./local_folder/
  ```

---

## কমান্ড সামারি টেবিল

| Command | Description |
| :--- | :--- |
| `docker exec -it` | রানিং কন্টেইনারের ভেতরে টার্মিনাল এক্সেস দেয় |
| `docker logs` | কন্টেইনারের আউটপুট বা লগ দেখায় |
| `docker logs -f` | লাইভ লগ আপডেট দেখায় |
| `docker stats` | মেমোরি ও সিপিইউ ব্যবহারের হিসাব দেখায় |
| `docker top` | কন্টেইনারের রানিং প্রসেস লিস্ট করে |
| `docker inspect` | কন্টেইনারের সব টেকনিক্যাল ডিটেইলস দেয় |
| `docker cp` | হোস্ট এবং কন্টেইনারের মধ্যে ফাইল কপি করে |

---

> [!TIP]
> কন্টেইনার থেকে বের হতে চাইলে কিবোর্ডে `exit` লিখুন অথবা `Ctrl + D` চাপুন।