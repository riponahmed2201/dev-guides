# Docker Context 🌐

আপনার নিজের মেশিন থেকে রিমোট কোনো সার্ভারের ডকার ইঞ্জিন কন্ট্রোল করার জন্য **Docker Context** ব্যবহার করা হয়। এটি মাল্টিপল ডকার হোস্ট ম্যানেজ করা অনেক সহজ করে দেয়।

---

## ১. Docker Context কি?

সাধারণত আমরা যখন `docker ps` বা `docker run` কমান্ড দেই, সেটি আমাদের লোকাল মেশিনের ডকার ইঞ্জিনে কাজ করে। কিন্তু যদি আপনার ৫টি আলাদা সার্ভার থাকে, তবে সেগুলোতে কাজ করার জন্য বারবার SSH করে ভেতরে ঢোকা বিরক্তিকর। 

**Docker Context** আপনাকে আপনার লোকাল টার্মিনাল থেকেই রিমোট সার্ভারগুলোকে সুইচ করার এবং কমান্ড দেওয়ার সুবিধা দেয়।

---

## ২. কেন Docker Context ব্যবহার করবেন?

- **একই সাথে মাল্টিপল হোস্ট:** লোকাল মেশিন, ক্লাউড সার্ভার (AWS/DigitalOcean), এবং স্টেজিন সার্ভারকে এক জায়গা থেকে কন্ট্রোল করা।
- **SSH ইন্টিগ্রেশন:** কোনো এক্সট্রা কনফিগারেশন ছাড়াই সরাসরি SSH প্রোটোকল ব্যবহার করে রিমোট ডকারে এক্সেস।
- **সহজ সুইচিং:** এক এনভায়রনমেন্ট থেকে অন্য এনভায়রনমেন্টে দ্রুত মুভ করা।

---

## ৩. রিমোট কানেকশন সেটআপ (Setup)

### SSH-এর মাধ্যমে কানেক্ট করা (সবচেয়ে সহজ পদ্ধতি)
রিমোট সার্ভারে যদি SSH এক্সেস থাকে, তবে নিচের কমান্ড দিয়ে প্রসংগ (Context) তৈরি করুন:

```bash
docker context create my-remote-server --docker "host=ssh://user@remote-ip"
```

### TLS-এর মাধ্যমে কানেক্ট করা
যদি সিকিউর ডকার পোর্ট (২৩৭৬) ওপেন থাকে এবং আপনার কাছে সার্টিফিকেট থাকে:
```bash
docker context create secure-server --docker "host=tcp://remote-ip:2376,ca=ca.pem,cert=cert.pem,key=key.pem"
```

---

## ৪. Context ম্যানেজমেন্ট কমান্ডসমূহ

- **সব কন্টেক্সট দেখা:**
  ```bash
  docker context ls
  ```

- **একটি কন্টেক্সট অ্যাক্টিভ করা:**
  ```bash
  docker context use my-remote-server
  ```
  *(এরপর থেকে আপনি যা কমান্ড দেবেন তা সরাসরি রিমোট সার্ভারে কাজ করবে)*

- **লোকাল হোস্টে ফিরে আসা:**
  ```bash
  docker context use default
  ```

- **কন্টেক্সট সম্পর্কে বিস্তারিত জানা:**
  ```bash
  docker context inspect my-remote-server
  ```

---

## ৫. রিমোট সার্ভারে ডকার ড্যামন (Daemon) সেটিংস

SSH ব্যবহার করলে রিমোট সার্ভারে ডকার ইন্সটল থাকা এবং ইউজারকে `docker` গ্রুপে থাকা ছাড়া আর তেমন কিছুই করতে হয় না। কিন্তু TLS ব্যবহার করলে রিমোট সার্ভারের ডকার কনফিগারেশনে গিয়ে পোর্ট ওপেন করতে হয়।

---

## সারসংক্ষেপ (Summary)
Docker Context হলো একজন ডেভলপার বা ডেভঅপ্স ইঞ্জিনিয়ারের জন্য একটি পাওয়ারফুল টুল। এটি আপনার প্রোডাক্টিভিটি বাড়ায় এবং রিমোট ইনফ্রাস্ট্রাকচার ম্যানেজমেন্টকে অনেক বেশি স্মার্ট করে তোলে।

---

> [!TIP]
> রিমোট কন্টেক্সট ব্যবহার করার সময় সব সময় সতর্ক থাকুন, কারণ ভুল করে লোকাল মনে করে প্রোডাকশন কন্টেইনার ডিলিট করে দেওয়ার সম্ভাবনা থাকে। সব সময় `docker context ls` দিয়ে চেক করে নিন আপনি কোন হোস্টে আছেন।
