# Docker APIs 🔌

ডকার শুধুমাত্র একটি কমান্ড লাইন টুল নয়, এটি একটি শক্তিশালী **RESTful API** প্রদান করে। এর মাধ্যমে আপনি যেকোনো প্রোগ্রমিং ল্যাঙ্গুয়েজ ব্যবহার করে ডকার ইঞ্জিনকে কন্ট্রোল করতে পারেন।

---

## ১. ডকার ইঞ্জিন API কি?

ডকার ক্লায়েন্ট (CLI) এবং ডকার ড্যামন (Daemon) একে অপরের সাথে কথা বলার জন্য একটি API ব্যবহার করে। আপনি যখনই `docker run` কমান্ড দেন, তখন CLI মূলত ডকার ইঞ্জিনের একটি API এন্ডপয়েন্টে রিকোয়েস্ট পাঠায়।

- এটি একটি **HTTP REST API**।
- এটি JSON ফরম্যাটে ডেটা রিটার্ন করে।
- ডিফল্টভাবে এটি একটি ইউনিক্স সকেট (`/var/run/docker.sock`) এর মাধ্যমে কাজ করে।

---

## ২. রিমোট API এক্সেস (Remote Access)

আপনি যদি অন্য মেশিন থেকে ডকার ইঞ্জিন কন্ট্রোল করতে চান, তবে আপনাকে TCP সকেট এনাবল করতে হবে।

> [!CAUTION]
> সরাসরি TCP পোর্ট ওপেন করা অত্যন্ত ঝুঁকিপূর্ণ। সব সময় **TLS (SSL)** সার্টিফিকেট ব্যবহার করে কানেকশন সিকিউর করুন।

---

## ৩. Docker SDKs

ডকার সরাসরি API কল করার পরিবর্তে ডেভলপারদের জন্য অফিসিয়াল লাইব্রেরি বা SDK প্রদান করে। এর মাধ্যমে কোড লেখা অনেক সহজ হয়।

- **Python SDK:** `pip install docker`
- **Go SDK:** `go get github.com/docker/docker/client`

### Python উদাহরণ:

```python
import docker
client = docker.from_env()

# নতুন কন্টেইনার রান করা
container = client.containers.run("nginx", detach=True)
print(f"Container {container.id} is running!")

# কন্টেইনার লিস্ট দেখা
for c in client.containers.list():
    print(c.name)
```

---

## ৪. API ভার্সনিং (Versioning)

ডকার ইঞ্জিনের সাথে সাথে এর API-এরও ভার্সন আপডেট হয়।

- আপনি `docker version` কমান্ড দিলে API ভার্সন দেখতে পাবেন।
- সব সময় লেটেস্ট বা স্টেবল ভার্সন ব্যবহার করার চেষ্টা করুন।
- ইউআরএল ফরম্যাট: `http://localhost/v1.43/containers/json`

---

## ৫. প্রোগ্রাম্যাটিক কন্ট্রোল (Programmatic Control)

কেন আপনি API ব্যবহার করবেন?

- **Automation:** ডকার কন্টেইনার অটোমেশন স্ক্রিপ্ট তৈরি করতে।
- **Cloud Management:** কাস্টম ড্যাশবোর্ড বা কন্টেইনার ম্যানেজমেন্ট প্ল্যাটফর্ম বানাতে।
- **CI/CD Build:** পাইপলাইনের ভেতরে কাস্টম কন্ডিশনের উপর ভিত্তি করে কন্টেইনার হ্যান্ডেল করতে।

---

## সারসংক্ষেপ (Summary)

ডকার API বুঝতে পারা মানে হলো ডকারের গভীরে প্রবেশ করা। এটি আপনাকে সাধারণ ইউজারের গণ্ডি পেরিয়ে একজন ইনফ্রাস্ট্রাকচার অথবা টুলস ডেভলপার হিসেবে গড়ে তুলতে সাহায্য করবে।

---

> [!IMPORTANT]
> প্রোডাকশন এনভায়রনমেন্টে ডকার সকেট (`docker.sock`) এক্সপোজ করার সময় অত্যন্ত সতর্ক থাকুন, কারণ এর মাধ্যমে পুরো হোস্টের এক্সেস পাওয়া সম্ভব।
