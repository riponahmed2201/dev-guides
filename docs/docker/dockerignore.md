# .dockerignore File 🚫

ডকার ইমেজ বিল্ড করার সময় আপনার লোকাল ডিরেক্টরি থেকে কোন ফাইলগুলো ডকার ইঞ্জিনে পাঠানো হবে না, তা নির্ধারণ করার জন্য `.dockerignore` ফাইল ব্যবহার করা হয়। এটি অনেকটা Git-এর `.gitignore` ফাইলের মতোই কাজ করে।

---

## ১. .dockerignore কেন প্রয়োজন?

যখন আপনি `docker build` কমান্ড চালান, ডকার আপনার কারেন্ট ডিরেক্টরি (যাকে **Build Context** বলা হয়) ডকার ডেমনের কাছে পাঠিয়ে দেয়। যদি আপনার ডিরেক্টরিতে বিশাল সাইজের ফাইল (যেমন: `node_modules`, বিশাল ডেটাসেট) থাকে, তবে:

- বিল্ড প্রসেস অনেক ধীর হয়ে যায়।
- ইমেজের সাইজ অযথাই বেড়ে যায়।
- সেনসিটিভ ডেটা (যেমন: `.env`, পাসওয়ার্ড) ইমেজে চলে যেতে পারে।

---

## ২. একটি সাধারণ .dockerignore ফাইলের গঠন

আপনার প্রজেক্টের রুট ডিরেক্টরিতে `.dockerignore` নামে একটি ফাইল তৈরি করুন এবং নিচের মতো নিয়মগুলো লিখুন:

```text
# ডিপেন্ডেন্সি বালাই
node_modules
npm-debug.log

# সোর্স কন্ট্রোল
.git
.gitignore

# লোকাল কনফিগারেশন ও সিক্রেটস
.env
*.local

# ওএস জেনারেটেড ফাইল
.DS_Store
Thumbs.db

# ডকুমেন্টেশন
README.md
*.md
```

---

## ৩. .dockerignore এর সুবিধা (Benefits)

- **Faster Builds:** বিল্ড কন্টেক্সট ছোট হওয়ায় ডকার ডেমনের কাছে ডেটা পাঠাতে কম সময় লাগে।
- **Smaller Images:** অপ্রয়োজনীয় ফাইল ইমেজে যোগ হয় না, ফলে ইমেজ সাইজ ছোট থাকে।
- **Security:** `.env` বা অন্যান্য কনফিডেনশিয়াল ফাইল ভুলবশত ইমেজের ভেতর চলে যাওয়া রোধ করে।
- **Clean Caching:** অপ্রয়োজনীয় ফাইলের পরিবর্তনের কারণে ডকার লেয়ার ক্যাশ ইনভ্যালিডেট হয় না।

---

## ৪. প্যাটার্ন ও রুলস (Rules)

`.dockerignore` ফাইলে আপনি নিচের প্যাটার্নগুলো ব্যবহার করতে পারেন:

| প্যাটার্ন    | মানে                                                                    |
| :----------- | :---------------------------------------------------------------------- |
| `*/temp*`    | যেকোনো সাব-ডিরেক্টরির temp দিয়ে শুরু হওয়া ফাইল/ফোল্ডার বাদ যাবে।       |
| `*/*.md`     | যেকোনো সাব-ডিরেক্টরির সব .md ফাইল বাদ যাবে।                             |
| `temp?`      | temp-এর পর একটি ক্যারেক্টার আছে এমন ফাইল বাদ যাবে (যেমন: temp1, tempA)। |
| `!README.md` | কোনো নিয়ম অনুযায়ী বাদ গেলেও এই ফাইলটি রাখা হবে (Exception)।             |

---

## সারসংক্ষেপ (Summary)

Dockerfile-এর `COPY . .` কমান্ডটি ব্যবহারের আগে অবশ্যই একটি সঠিক `.dockerignore` ফাইল সেট করে নিন। এটি আপনার কন্টেইনারাইজেশন প্রসেসকে আরও প্রফেশনাল এবং সিকিউর করবে।

---

> [!TIP]
> সবসময় `node_modules` এবং `.git` ফোল্ডার দুটোকে `.dockerignore`-এ রাখা বাধ্যতামূলক বলে মনে করুন!
