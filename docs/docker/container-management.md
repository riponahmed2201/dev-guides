# Container Management Commands 🛠️

কন্টেইনার তৈরি করার পর সেগুলোকে নিয়মিত ম্যানেজ করা, স্ট্যাটাস দেখা এবং প্রয়োজন শেষে মুছে ফেলার জন্য কিছু নির্দিষ্ট কমান্ড ব্যবহার করতে হয়। এই গাইডে আমরা কন্টেইনার ম্যানেজমেন্টের সব জরুরি কমান্ড নিয়ে আলোচনা করবো।

---

## ১. কন্টেইনার লিস্ট দেখা (List Containers)

আপনার সিস্টেমে কোন কোন কন্টেইনার আছে তা জানার জন্য:

- **শুধুমাত্র রানিং কন্টেইনার দেখতে:**
  ```bash
  docker ps
  ```
- **সব কন্টেইনার (রানিং এবং স্টপড) দেখতে:**
  ```bash
  docker ps -a
  ```
  এটি আপনাকে কন্টেইনার আইডি, ইমেজ নেম, স্ট্যাটাস এবং পোর্ট ম্যাপিং দেখাবে।

---

## ২. কন্টেইনার স্টার্ট এবং স্টপ (Start/Stop)

- **বন্ধ কন্টেইনার চালু করতে:**
  ```bash
  docker start <container_id_or_name>
  ```
- **রানিং কন্টেইনার থামিয়ে দিতে:**
  ```bash
  docker stop <container_id_or_name>
  ```
- **কন্টেইনার রিস্টার্ট করতে:**
  ```bash
  docker restart <container_id_or_name>
  ```

---

## ৩. কন্টেইনার ফোর্স স্টপ (Force Kill)

মাঝে মাঝে `docker stop` কমান্ড দিয়ে কন্টেইনার বন্ধ হতে সময় নেয় অথবা হ্যাং হয়ে থাকে। সেক্ষেত্রে জোরপূর্বক বন্ধ করতে:

```bash
docker kill <container_id_or_name>
```

---

## ৪. কন্টেইনার পজ এবং আনপজ (Pause/Unpause)

যদি আপনি কন্টেইনারটি পুরোপুরি বন্ধ না করে এর প্রোসেসগুলো সাময়িকভাবে থামিয়ে রাখতে চান (RAM-এ থাকবে কিন্তু CPU ব্যবহার করবে না):

- **পজ করতে:**
  ```bash
  docker pause <container_id_or_name>
  ```
- **পুনরায় চালু করতে:**
  ```bash
  docker unpause <container_id_or_name>
  ```

---

## ৫. কন্টেইনার মুছে ফেলা (Remove)

অপ্রয়োজনীয় কন্টেইনার ডিলিট করার জন্য:

```bash
docker rm <container_id_or_name>
```

> [!WARNING]
> কন্টেইনার ডিলিট করার আগে অবশ্যই সেটি `stop` করে নিতে হবে। রানিং কন্টেইনার ফোর্সফুলি ডিলিট করতে চাইলে `-f` ফ্ল্যাগ ব্যবহার করা যায়: `docker rm -f <container_id>`.

---

## ৬. এক্সিট হওয়া সব কন্টেইনার একসাথে মোছা

আপনার সিস্টেমে যদি অনেকগুলো বন্ধ হয়ে থাকা (Exited) কন্টেইনার জমে যায়, তবে সেগুলো একবারে পরিষ্কার করতে:

```bash
docker container prune
```

---

## কমান্ড সামারি টেবিল

| Command          | Description                     |
| :--------------- | :------------------------------ |
| `docker ps`      | রানিং কন্টেইনার লিস্ট করে       |
| `docker ps -a`   | সব কন্টেইনার লিস্ট করে          |
| `docker start`   | কন্টেইনার চালু করে              |
| `docker stop`    | কন্টেইনার থামায় (Graceful)      |
| `docker kill`    | কন্টেইনার থামায় (Forceful)      |
| `docker restart` | কন্টেইনার রিস্টার্ট করে         |
| `docker pause`   | কন্টেইনারের প্রোসেস থামিয়ে রাখে |
| `docker unpause` | পজ হওয়া কন্টেইনার চালু করে      |
| `docker rm`      | কন্টেইনার চিরতরে মুছে ফেলে      |

---

> [!TIP] > `docker ps` কমান্ডে আইডিগুলো অনেক বড় দেখায়। আপনি চাইলে `docker ps --format` ব্যবহার করে শুধুমাত্র প্রয়োজনীয় কলামগুলো সুন্দরভাবে দেখতে পারেন।
