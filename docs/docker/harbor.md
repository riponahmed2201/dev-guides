# Harbor Registry 🚢

এন্টারপ্রাইজ লেভেলে ডকার ইমেজ ম্যানেজ করার জন্য বর্তমানে সবচেয়ে জনপ্রিয় ওপেন সোর্স সলিউশন হলো **Harbor**। এটি একটি ক্লাউড নেটিভ রেজিস্ট্রি প্রজেক্ট যা ইমেজ সিকিউরিটি এবং ম্যানেজমেন্টকে এক ধাপ এগিয়ে নিয়ে যায়।

---

## ১. Harbor কি?

Harbor হলো একটি ওপেন-সোর্স ট্রাস্টেড ক্লাউড নেটিভ রেজিস্ট্রি যা ডকার ইমেজ এবং হেলম চার্ট (Helm Charts) স্টোর করে। এটি শুধুমাত্র ইমেজ জমা রাখে না, বরং ইমেজের নিরাপত্তা এবং পারমিশন ম্যানেজমেন্টও নিশ্চিত করে।

---

## ২. কেন Harbor ব্যবহার করবেন? (Key Features)

Harbor-এ এমন কিছু অ্যাডভান্সড ফিচার আছে যা স্ট্যান্ডার্ড ডকার রেজিস্ট্রিতে (`registry:2`) থাকে না:

- **Web User Interface (UI):** ইজি-টু-ইউজ গ্রাফিক্যাল ইন্টারফেস যার মাধ্যমে ব্রাউজার থেকেই ইমেজ এবং প্রোজেক্ট ম্যানেজ করা যায়।
- **Vulnerability Scanning:** এটি অটোমেটিকালি ইমেজের ভেতর সিকিউরিটি বাগ বা CVE স্ক্যান করে (Trivy ইঞ্জিন ব্যবহার করে)।
- **RBAC (Role-Based Access Control):** আপনি কন্ট্রোল করতে পারেন কোন ইউজার কোন ইমেজে পুশ বা পুল করতে পারবে।
- **Content Signing (Notary):** ইমেজের অরিজিনালতা নিশ্চিত করতে ডিজিটাললি সাইন করা যায়।
- **Replication:** একটি নির্দিষ্ট ইমেজ এক রেজিস্ট্রি থেকে অন্য রেজিস্ট্রিতে অটোমেটিক কপি করা যায়।

---

## ৩. প্রোজেক্ট এবং মাল্টি-টেন্যান্সি (Multi-tenancy)

Harbor-এ ইমেজগুলোকে বিভিন্ন **Project**-এর আন্ডারে রাখা হয়। এর ফলে একই সার্ভারে বিভিন্ন টিমের ইমেজ আলাদা আলাদা আইসোলেটেড অবস্থায় রাখা সম্ভব।

- **Public Projects:** যে কেউ ইমেজ পুল করতে পারে।
- **Private Projects:** শুধুমাত্র অনুমোদিত ইউজাররাই ইউজ করতে পারে।

---

## ৪. সিকিউরিটি এবং অডিট (Security & Audit)

- **CVE Detection:** ইমেজ পুশ করার সাথে সাথেই Harbor সেটিকে স্ক্যান করে একটি রিপোর্ট দেয়। কোনো ইমেজে যদি "Critical" রিস্ক থাকে, তবে সেটি পুল করা ব্লক করে দেওয়া যায়।
- **Audit Logs:** কে কখন কোন ইমেজে এক্সেস করেছে তার পূর্ণাঙ্গ লগ Harbor-এ জমা থাকে।

---

## ৫. হারবার ইন্সটলেশন (Installation)

Harbor সাধারণত **Docker Compose** বা **Kubernetes (Helm)** ব্যবহার করে ইন্সটল করা হয়। এটি ইন্সটল করার জন্য একটি প্রি-রেকুইজিট স্ক্রিপ্ট ও কনফিগারেশন ফাইল (`harbor.yml`) থাকে।

---

## ৬. ইমেজ পুশ করা (Pushing Image)

Harbor-এ ইমেজ পুশ করার পদ্ধতি স্ট্যান্ডার্ড ডকার রেজিস্ট্রির মতোই:

```bash
# ১. লগইন করা
docker login harbor.example.com

# ২. ইমেজ ট্যাগ করা
docker tag my-app:v1 harbor.example.com/my-project/my-app:v1

# ৩. পুশ করা
docker push harbor.example.com/my-project/my-app:v1
```

---

## সারসংক্ষেপ (Summary)

যদি আপনার অফিস বা প্রোজেক্টে অনেক বেশি ডকার ইমেজ এবং মাল্টি-টিম কোলাবরেশন প্রয়োজন হয়, তবে Harbor হলো এক কথায় সেরা সলিউশন। এটি আপনার ইমেজ স্টোরেজকে যেমন সিকিউর করে, তেমনি ম্যানেজমেন্টকে করে তোলে অত্যন্ত সহজ।

---

> [!TIP]
> প্রোডাকশন ক্লাস্টার (যেমন: Kubernetes) ব্যবহারের সময় Harbor-কে ইমেজ স্টোর হিসেবে ব্যবহার করা একটি স্ট্যান্ডার্ড প্র্যাকটিস।
