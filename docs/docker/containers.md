# Running Containers 🚀

Docker-এর সবচেয়ে বেশি ব্যবহৃত এবং শক্তিশালী কমান্ড হলো `docker run`। এই কমান্ডটি একটি ইমেজ থেকে নতুন কন্টেইনার তৈরি করে এবং সেটি স্টার্ট করে। এই গাইডে আমরা `docker run`-এর বিভিন্ন অপশন এবং ফ্ল্যাগ নিয়ে বিস্তারিত আলোচনা করবো।

---

## ১. বেসিক রান (Basic Run)

সবচেয়ে সহজভাবে একটি কন্টেইনার চালানোর নিয়ম:
```bash
docker run <image_name>
```
এটি ইমেজটি ডাউনলোড করবে (যদি লোকাল মেশিনে না থাকে), কন্টেইনার তৈরি করবে এবং সেটি রান করবে।

---

## ২. ডিটাচড মোড (Detached Mode: -d)

টার্মিনাল ব্লক না করে ব্যাকগ্রাউন্ডে কন্টেইনার চালানোর জন্য:
```bash
docker run -d <image_name>
```
সচরাচর আমরা যখন কোনো ডাটাবেস বা ওয়েব সার্ভার চালাই, তখন এই মোডটি ব্যবহার করি।

---

## ৩. ইন্টারঅ্যাক্টিভ মোড (Interactive Mode: -it)

যদি আপনি কন্টেইনারের ভেতরে ইনপুট দিতে চান বা এর টার্মিনাল এক্সেস করতে চান:
```bash
docker run -it <image_name> /bin/bash
```
- `-i`: Interactive (ইনপুট নিতে পারবে)।
- `-t`: TTY (একটি টার্মিনাল উইন্ডো দিবে)।

---

## ৪. কন্টেইনারের নাম দেওয়া (Named Container: --name)

ডকার অটোমেটিক কন্টেইনারের জন্য একটি অদ্ভুত নাম দিয়ে দেয়। আপনার নিজের পছন্দমতো নাম দেওয়ার জন্য:
```bash
docker run --name my-website nginx
```
এটি ম্যানেজ করা অনেক সহজ করে দেয়।

---

## ৫. পোর্ট ম্যাপিং (Port Mapping: -p)

কন্টেইনারের ভেতরের সার্ভিসকে আপনার কম্পিউটার বা ইন্টারনেটে এক্সেস করার জন্য পোর্ট ম্যাপিং দরকার:
```bash
docker run -p <Host_Port>:<Container_Port> <image_name>
```
**উদাহরণ:**
```bash
docker run -p 8080:80 nginx
```
এখন আপনি আপনার ব্রাউজারে `localhost:8080` লিখে এনজিনএক্স (Nginx) এক্সেস করতে পারবেন।

---

## ৬. এনভায়রনমেন্ট ভেরিয়েবল (Env Variables: -e)

কন্টেইনারের ভেতরে কোনো কনফিগারেশন বা সিক্রেট (যেমন: ডাটাবেস পাসওয়ার্ড) পাঠানোর জন্য:
```bash
docker run -e MYSQL_ROOT_PASSWORD=my-secret-pw mysql
```

---

## ৭. ভলিউম মাউন্ট (Volume Mount: -v)

কন্টেইনার ডিলিট হয়ে গেলেও ডেটা সেভ রাখার জন্য হোস্ট মেশিনের কোনো ফোল্ডারের সাথে কন্টেইনারকে লিঙ্ক করা হয়:
```bash
docker run -v /my/host/folder:/container/folder <image_name>
```

---

## ৮. অটো-রিমুভ (Auto-remove: --rm)

কাজ শেষ হয়ে যাওয়ার পর বা কন্টেইনার বন্ধ হয়ে গেলে সেটি অটোমেটিক ডিলিট করে দেওয়ার জন্য:
```bash
docker run --rm <image_name>
```
এটি সিস্টেম পরিষ্কার রাখতে সাহায্য করে, বিশেষ করে টেস্টিংয়ের সময়।

---

## ৯. কমন ফ্ল্যাগস সামারি (Summary Table)

| Flag | Full Name | Purpose |
| :--- | :--- | :--- |
| `-d` | Detached | কন্টেইনার ব্যাকগ্রাউন্ডে চলবে |
| `-it` | Interactive + TTY | টার্মিনাল এক্সেস দিবে |
| `--name` | Name | কন্টেইনারের নাম সেট করবে |
| `-p` | Port | হোস্ট এবং কন্টেইনারের পোর্ট লিঙ্ক করবে |
| `-e` | Env | এনভায়রনমেন্ট ভেরিয়েবল পাস করবে |
| `-v` | Volume | ডেটা পারসিস্টেন্স বা শেয়ার করবে |
| `--rm` | Remove | স্টপ হওয়ার সাথে সাথে কন্টেইনার ডিলিট করবে |

---

> [!TIP]
> `docker run` আসলে দুটি কমান্ডের সমষ্টি: `docker create` এবং `docker start`।