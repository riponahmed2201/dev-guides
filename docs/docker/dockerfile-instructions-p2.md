# Dockerfile Instructions - Part 2 🛠️

Dockerfile-এর প্রথম পার্টে আমরা বেসিক কিছু ইন্সট্রাকশন শিখেছি। এই পার্টে আমরা আরও কিছু অ্যাডভান্সড ইন্সট্রাকশন নিয়ে আলোচনা করবো যেগুলো কন্টেইনারের কনফিগারেশন, সিকিউরিটি এবং ডেটা ম্যানেজমেন্টের জন্য অত্যন্ত গুরুত্বপূর্ণ।

---

## ১. ENV vs ARG (ভেরিয়েবল ম্যানেজমেন্ট)

ডকারে দুই ধরনের ভেরিয়েবল ব্যবহার করা হয়:

- **`ENV` (Environment Variable):** এটি ইমেজ বিল্ড হওয়ার সময় এবং কন্টেইনার রান করার সময়—উভয় ক্ষেত্রেই পাওয়া যায়।
  ```dockerfile
  ENV APP_COLOR=blue
  ```
- **`ARG` (Build-time Variable):** এটি শুধুমাত্র ইমেজ বিল্ড হওয়ার সময় কাজ করে। কন্টেইনার রান হয়ে গেলে এই ভেরিয়েবল আর পাওয়া যায় না।
  ```dockerfile
  ARG VERSION=1.0
  ```

---

## ২. EXPOSE (পোর্ট ডকুমেন্টেশন)

আপনার কন্টেইনারটি কোন পোর্টে লিসেন (Listen) করবে সেটি জানানোর জন্য `EXPOSE` ব্যবহৃত হয়।
```dockerfile
EXPOSE 8080
```
> [!NOTE]
> মনে রাখবেন, `EXPOSE` কমান্ডটি নিজে থেকে পোর্ট ওপেন করে না। এটি শুধুমাত্র ডকুমেন্টেশন হিসেবে কাজ করে। পোর্ট ওপেন করতে হলে রান করার সময় `-p` ফ্ল্যাগ ব্যবহার করতে হয়।

---

## ৩. VOLUME (মাউন্ট পয়েন্ট)

কন্টেইনারের ভেতরের কোনো ফোল্ডারকে হোস্ট মেশিনের সাথে কানেক্ট করার জন্য বা ডেটা সেভ রাখার জন্য `VOLUME` ব্যবহার করা হয়।
```dockerfile
VOLUME ["/data"]
```
এটি কন্টেইনার ডিলিট হয়ে গেলেও আপনার মূল্যবান ডেটা (যেমন: ডাটাবেস ফাইল) হারিয়ে যাওয়া থেকে রক্ষা করে।

---

## ৪. USER (সিকিউরিটি)

ডিফল্টভাবে ডকার কন্টেইনার `root` ইউজার হিসেবে চলে, যা সিকিউরিটির জন্য ঝুঁকিপূর্ণ। কন্টেইনারে কম পাওয়ারফুল কোনো ইউজারে সুইচ করতে `USER` ব্যবহার করা হয়।
```dockerfile
RUN useradd -m myuser
USER myuser
```

---

## ৫. ENTRYPOINT vs CMD (সবচেয়ে কনফিউজিং পার্ট)

এই দুটি ইন্সট্রাকশনের পার্থক্য বোঝা ডকার শেখার জন্য খুবই জরুরি:

- **`ENTRYPOINT`**: এটি একটি ফিক্সড কমান্ড নির্ধারণ করে। আপনি কন্টেইনার রান করার সময় বাইরে থেকে এটি সহজে পরিবর্তন করতে পারবেন না।
  ```dockerfile
  ENTRYPOINT ["python"]
  ```
- **`CMD`**: এটি মূলত `ENTRYPOINT`-এর জন্য ডিফল্ট আর্গুমেন্ট হিসেবে কাজ করে। রান করার সময় বাইরে থেকে আপনি এটি সহজেই চেঞ্জ করতে পারেন।
  ```dockerfile
  CMD ["app.py"]
  ```

**একসাথে ব্যবহার করলে:**
```dockerfile
ENTRYPOINT ["python"]
CMD ["app.py"]
```
এখন রান করার সময়:
- `docker run my-image` দিলে চলবে: `python app.py`
- `docker run my-image script.py` দিলে চলবে: `python script.py` (এখানে `CMD`-কে ওভাররাইড করা হয়েছে)।

---

## উদাহরণ প্রোজেক্ট (Advanced Dockerfile)

```dockerfile
FROM python:3.9-slim

# ১. এনভায়রনমেন্ট সেট আপ
ENV PYTHONUNBUFFERED=1

# ২. ওয়ার্ক ডিরেক্টরি
WORKDIR /code

# ৩. ইউজার তৈরি এবং সুইচ
RUN useradd -m myuser
USER myuser

# ৪. ফাইল কপি
COPY . .

# ৫. পোর্ট
EXPOSE 5000

# ৬. এন্ট্রি পয়েন্ট এবং ডিফল্ট কমান্ড
ENTRYPOINT ["python"]
CMD ["main.py"]
```

---

> [!CAUTION]
> সিকিউরিটির খাতিরে প্রোডাকশন ইমেজে সবসময় `USER` ইন্সট্রাকশন ব্যবহার করে রুট এক্সেস বন্ধ রাখা উচিত।
