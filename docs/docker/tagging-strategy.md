# Docker Image Tagging Strategy 🏷️

ডকার ইমেজ ম্যানেজ করার ক্ষেত্রে সঠিক **Tagging Strategy** অনুসরণ করা অত্যন্ত গুরুত্বপূর্ণ। এটি ছাড়া ভার্সন কন্ট্রোল এবং প্রোডাকশন রোলব্যাক করা প্রায় অসম্ভব হয়ে পড়ে।

---

## ১. কেন ইমেজে ট্যাগ (Tag) প্রয়োজন?

ট্যাগ হলো একটি ইমেজের ডেসিগনেশন বা মার্কার। এটি ছাড়া ডকার ইঞ্জিন বুঝতে পারে না আপনি ইমেজের কোন ভার্সনটি ইউজ করতে চাচ্ছেন। সঠিক ট্যাগিং স্ট্রেটেজি থাকলে আপনি সহজেই বুঝতে পারবেন কোন ইমেজটি কোন এনভায়রনমেন্ট বা ফিচারের জন্য তৈরি।

---

## ২. জনপ্রিয় ট্যাগিং স্ট্রেটেজি (Strategies)

### ক. Semantic Versioning (SemVer)
সফটওয়্যার ইঞ্জিনিয়ারিংয়ের স্ট্যান্ডার্ড নিয়ম। ফরম্যাট: `MAJOR.MINOR.PATCH` (যেমন: `v1.2.3`)
- **MAJOR:** যখন বড় কোনো পরিবর্তন হয়।
- **MINOR:** যখন নতুন কোনো ফিচার যোগ হয় কিন্তু পুরনো কোডের সাথে সামঞ্জস্য থাকে।
- **PATCH:** শুধুমাত্র বাগ ফিক্সের জন্য।

### খ. Git Commit SHA
সবচেয়ে নিখুঁত পদ্ধতি হলো ইমেজের সাথে গিটের কমিট আইডি (Git SHA) যোগ করা।
- উদাহরণ: `my-app:8f9a2b4`
- সুবিধা: কোনো সমস্যা হলে সহজেই ডেরাইভ করা যায় কোন কমিট থেকে ইমেজটি তৈরি হয়েছে।

### গ. Build Number
CI/CD পাইপলাইনের বিল্ড নাম্বার ইউজ করা।
- উদাহরণ: `my-app:build-205`

### ঘ. Environment Tags
এনভায়রনমেন্ট অনুযায়ী ট্যাগ করা।
- উদাহরণ: `prod-v1.0`, `staging-latest`, `dev`

---

## ৩. `latest` ট্যাগের ঝুঁকি (Pros and Cons)

ডকার ডিফল্টভাবে `latest` ট্যাগ ব্যবহার করে।
- **Pros:** সব সময় লেটেস্ট কপি পাওয়া যায়।
- **Cons:** এটি "Immutable" নয়। আজকে যা `latest`, কাল তা অন্যকিছু হতে পারে। প্রোডাকশনে `latest` ব্যবহার করলে হঠাৎ করে অ্যাপ ব্রেক হতে পারে এবং রোলব্যাক করা কঠিন হয়।

**বেস্ট প্র্যাকটিস:** প্রোডাকশনে সব সময় স্পেসিফিক ভার্সন ট্যাগ (যেমন: `v1.2.0`) ব্যবহার করুন।

---

## ৪. Immutable Tags

একটি ভালো স্ট্রেটেজি হলো ইমেজের ট্যাগ কখনোই ওভাররাইট না করা। প্রতিবার বিল্ড করার সময় নতুন একটি ইউনিক ট্যাগ দেওয়া উচিত। একে বলা হয় **Immutable Tagging**।

---

## ৫. ট্যাগ নেমিং কনভেনশন (Naming Conventions)

- ছোট হাতের অক্ষর ব্যবহার করুন (Lowercase)।
- স্পেশাল ক্যারেক্টার হিসেবে হাইফেন (`-`) বা ডট (`.`) ইউজ করুন।
- ট্যাগ যেন ছোট এবং অর্থপূর্ণ হয়।
- উদাহরণ: `app-web-v1.2-stable`

---

## সারসংক্ষেপ (Summary)
একটি গোছানো ট্যাগিং স্ট্রেটেজি আপনার CI/CD পাইপলাইনকে আরও শক্তিশালী করে এবং প্রোডাকশন এনভায়রনমেন্টে ভুল হওয়ার সম্ভাবনা কমিয়ে দেয়। সব সময় SemVer অথবা Git SHA ব্যবহার করার চেষ্টা করুন।

---

> [!CAUTION]
> প্রোডাকশনে কখনোই শুধু `latest` ট্যাগের উপর নির্ভর করবেন না। এটি আপনার অ্যাপ্লিকেশনের স্ট্যাবিলিটি নষ্ট করতে পারে।
