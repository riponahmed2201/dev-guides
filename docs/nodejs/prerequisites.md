# Prerequisites & Setup (প্রস্তুতি ও সেটআপ)

Node.js শেখার যাত্রায় স্বাগতম! কোড লেখা শুরু করার আগে, আমাদের বুঝতে হবে Node.js আসলে কি, এটি কেন এত জনপ্রিয়, এবং আমাদের কম্পিউটারে এটি ব্যবহারের জন্য কিভাবে প্রস্তুত বা সেটআপ করতে হয়।

## ১. Node.js কি এবং কেন?

সহজ ভাষায়, **Node.js** কোনো প্রোগ্রামিং ল্যাঙ্গুয়েজ নয় (যেমন Python বা Java), আবার এটি কোনো ফ্রেমওয়ার্কও নয় (যেমন React বা Angular)। এটি একটি **JavaScript Runtime Environment**।

### একটু গভীরে বোঝা যাক: JavaScript Runtime Environment
সাধারণত, JavaScript ডোমেইন বা ব্রাউজারের (Chrome, Firefox) ভেতরে রান করে। ব্রাউজারের এই পরিবেশকে বলা হয় **Client-side Runtime**। কিন্তু ব্রাউজারের বাইরে, যেমন আপনার কম্পিউটারের টার্মিনালে বা কোনো সার্ভারে JavaScript রান করানোর ক্ষমতা এর আগে ছিল না। 

Node.js এই সীমাবদ্ধতা দূর করেছে। এটি JavaScript-কে ব্রাউজারের বাইরে রান করানোর জন্য একটি **Server-side Runtime Environment** প্রদান করে।

---

## ২. Node.js vs Browser JavaScript (পার্থক্য)

অনেকেই গুলিয়ে ফেলেন যে ব্রাউজারের জাভাস্ক্রিপ্ট আর নোড জেএস এর জাভাস্ক্রিপ্ট হয়তো আলাদা। আসলে ল্যাঙ্গুয়েজ একই, কিন্তু তাদের **Environment (পরিবেশ)** আলাদা।

| বৈশিষ্ট্য | Browser JavaScript | Node.js Environment |
| :--- | :--- | :--- |
| **DOM Access** | ব্রাউজারে `document` বা `window` অবজেক্ট আছে, যা দিয়ে HTML ম্যানিপুলেট করা যায়। | Node.js এ কোনো `window` বা `document` নেই। এখানে DOM ম্যানিপুলেশন করা যায় না। |
| **File System** | ব্রাউজার সিকিউরিটির কারণে ইউজারের কম্পিউটারের ফাইল সিস্টেমে হাত দিতে পারে না (ফাইল রিড/রাইট)। | Node.js দিয়ে কম্পিউটারের ফাইল তৈরি, পড়া বা ডিলিট করা যায় (`fs` মডিউল দিয়ে)। |
| **Global Object** | এখানে গ্লোবাল অবজেক্ট হলো `window`। | এখানে গ্লোবাল অবজেক্ট হলো `global`। |
| **Modules** | মডার্ন ব্রাউজারে ES6 Modules (`import`/`export`) সাপোর্ট করে। | Node.js মূলত CommonJS (`require`/`module.exports`) ব্যবহার করে, তবে এখন ES6-ও সাপোর্ট করে। |
| **Control** | ইউজারের ব্রাউজারের ওপর আপনার নিয়ন্ত্রণ নেই (কে কোন ভার্সন চালাচ্ছে)। | সার্ভারের এনভায়রনমেন্টের ওপর আপনার পূর্ণ নিয়ন্ত্রণ থাকে। |

---

## ৩. V8 Engine সম্পর্কে ধারণা

Node.js এর হৃদপিণ্ড হলো Google-এর **V8 JavaScript Engine**। এটি Google Chrome ব্রাউজারেও ব্যবহৃত হয়।

- **কাজ:** V8 ইঞ্জিন আমাদের লেখা JavaScript কোডকে কম্পিউটারের বোধগম্য **Machine Code**-এ দ্রুত রূপান্তর করে।
- **জাদু:** জাভাস্ক্রিপ্ট একটি ইন্টারপ্রেটেড ল্যাঙ্গুয়েজ হলেও, V8 ইঞ্জিন **Just-In-Time (JIT) compilation** প্রযুক্তি ব্যবহার করে কোড রান করার ঠিক আগেই কম্পাইল করে, ফলে পারফরম্যান্স হয় দুর্দান্ত।

---

## ৪. Environment Setup (ইনস্টলেশন)

সরাসরি Node.js ইনস্টল করার চেয়ে **NVM (Node Version Manager)** ব্যবহার করা বাঞ্ছনীয়। কারণ এটি আপনাকে একই মেশিনে Node.js এর একাধিক ভার্সন ব্যবহার করার সুবিধা দেয়।

### ধাপ ১: IDE/Editor সেটআপ
কোড লেখার জন্য একটি ভালো এডিটর প্রয়োজন। **VS Code (Visual Studio Code)** বর্তমান সময়ের সেরা এবং এটি সম্পূর্ণ ফ্রি।
- [Download VS Code](https://code.visualstudio.com/)

### ধাপ ২: Node.js ইনস্টলেশন (NVM এর মাধ্যমে)

#### Windows ব্যবহারকারীদের জন্য:
1. **nvm-windows** এর [লেটেস্ট রিলিজ](https://github.com/coreybutler/nvm-windows/releases) পেজ থেকে `nvm-setup.exe` ফাইলটি ডাউনলোড করে ইনস্টল করুন।
2. টার্মিনাল (Command Prompt বা PowerShell) ওপেন করে চেক করুন:
   ```bash
   nvm version
   ```
3. Node.js এর লেটেস্ট LTS (Long Term Support) ভার্সন ইনস্টল করুন:
   ```bash
   nvm install lts
   ```
4. ইনস্টল করা ভার্সনটি ব্যবহার করা শুরু করতে:
   ```bash
   nvm use lts
   ```

#### Mac/Linux ব্যবহারকারীদের জন্য:
টার্মিনালে নিচের কমান্ডটি রান করুন:
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
```
এরপর লিনাক্স/ম্যাকের নির্দেশ অনুযায়ী প্রোফাইল রিলোড করে `nvm install --lts` কমান্ড দিন।

### ইনস্টলেশন ভেরিফাই করা
ইনস্টলেশন সফল হয়েছে কিনা দেখতে টার্মিনালে লিখুন:
```bash
node -v
# আউটপুট হিসেবে ভার্সন দেখাবে, যেমন: v20.10.0

npm -v
# এনপিএম ভার্সন দেখাবে, যেমন: 10.2.3
```

---

## ৫. প্রথম "Hello World" প্রোগ্রাম

আমাদের সেটআপ রেডি! এখন চলুন আমাদের প্রথম Node.js কোড রান করি।

১. আপনার কম্পিউটারের যেকোনো জায়গায় একটি ফোল্ডার তৈরি করুন, নাম দিন `node-learning`।
২. VS Code দিয়ে ফোল্ডারটি ওপেন করুন।
৩. `index.js` নামে একটি নতুন ফাইল তৈরি করুন।
৪. ফাইলের ভেতর নিচের কোডটি লিখুন:

```javascript
console.log("Hello from Node.js! 🚀");
```

৫. এবার VS Code এর টার্মিনাল ওপেন করুন (`Ctrl + backtick` বা `Terminal > New Terminal`)।
৬. কোডটি রান করতে লিখুন:

```bash
node index.js
```

**আউটপুট:**
```
Hello from Node.js! 🚀
```

অভিনন্দন! 🎉 আপনি আপনার প্রথম Node.js প্রোগ্রাম সফলভাবে রান করেছেন।

---

## ৬. REPL (Read-Eval-Print-Loop)

Node.js এর সাথে বিল্ট-ইন একটি ইন্টারেক্টিভ শেল আসে যার নাম **REPL**। ছোটখাটো কোড টেস্ট করার জন্য এটি দারুণ।

ব্যবহারবিধি:
১. টার্মিনালে শুধু `node` লিখে এন্টার দিন।
২. এবার যেকোনো জাভাস্ক্রিপ্ট কোড লিখুন, সাথে সাথে রেজাল্ট পাবেন।

```bash
$ node
> 5 + 10
15
> const name = "Ripon";
undefined
> console.log(`Hello ${name}`);
Hello Ripon
undefined
>
```

REPL থেকে বের হতে `Ctrl + C` দুইবার চাপুন অথবা `.exit` লিখুন।
